name: Packer Build (bad — local build, no framework reusable)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Build locally instead of delegating to framework
        run: |
          echo "This caller does not reference the framework reusable build workflow."
          echo "Contract requires reusable-packer-framework-build.yaml@<sha>."
