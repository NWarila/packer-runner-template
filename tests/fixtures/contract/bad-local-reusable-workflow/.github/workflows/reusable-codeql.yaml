name: Reusable CodeQL (bad — local reusable implementation in runner)

# Runner repos must NOT host any local reusable workflows. The contract's
# forbidden_paths glob `.github/workflows/reusable-*.yaml` (no exceptions —
# including reusable-auto-merge.yaml) rejects this.

on:
  workflow_call:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "this should not exist in a runner repo"
